/**
 * GradientWrapper.js
 * 
 * This file provides a properly formatted LinearGradient export for react-native-gifted-charts
 */

import { SafeLinearGradient } from './SafeModules';

// Export in the format that react-native-gifted-charts expects
export const LinearGradient = SafeLinearGradient;

// Also provide a default export for compatibility
export default SafeLinearGradient;
